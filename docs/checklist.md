# 마감까지 할 일 전부 — 7/20 ~ 7/26

**팀 제주 BTS · 소리봄**

> 하나씩 지우면서 진행하세요. `- [ ]` 를 `- [x]` 로 바꾸면 됩니다.
> **이름이 붙은 항목은 그 사람이 책임집니다.** 못 하겠으면 미루지 말고 그날 안에 말하세요.

| | |
|---|---|
| 🔨 **개발 마감** | **7/24 (금) 저녁** — 이후 새 기능 없음 |
| 📤 **제출 마감** | **7/26 (일) 23:59** |

---

# 7/20 (월) — 각자 한 조각

## 아침에 다 같이 (30분)

- [ ] Jetson에서 최신 코드 받기
  ```bash
  cd /mnt/ssd/code/soribom && git fetch origin && git checkout jueon && git pull
  ```
- [ ] 각자 **본인 이름으로** 커밋되게 설정 (Jetson은 계정을 공유하므로 매번 확인)
  ```bash
  git config user.name "본인_GitHub_아이디"
  git config user.email "본인_이메일"
  git config user.name && git config user.email
  ```
- [ ] `docs/schedule.md` 와 이 문서를 다 같이 읽기
- [ ] **오늘 누가 무엇을 하는지** 서로 말로 확인

## 강주언 — 환경 · TTS

- [ ] `src/config.yaml` 검토하고 틀린 값 고치기 (어제 실측값이 들어가 있음)
- [ ] Claude Code 설치 (선택)
  ```bash
  curl -fsSL https://claude.ai/install.sh | bash
  echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc
  ```
- [ ] MeloTTS 설치 시도
- [ ] **30분 넘게 막히면 즉시 `espeak-ng` 로 전환** — 어제 이미 설치됨
  ```bash
  espeak-ng -v ko "안녕하세요. 질문이 있습니다." -w out.wav
  ```
- [ ] 만들어진 `.wav` 를 재생해서 실제로 들리는지 확인
- [ ] `src/tts/speaker.py` 에 함수 하나 완성: 글자를 넣으면 `.wav` 가 나온다
- [ ] **오늘의 성공:** 타이핑한 한국어가 소리 파일이 된다

## 강구현 — 자막이 계속 나오게

- [ ] `sounddevice` 로 마이크에서 소리를 계속 받기 (`hw:2,0`, 6채널, 16kHz)
- [ ] 6채널 중 **0번만** 꺼내 쓰기
- [ ] Silero VAD 붙이기 — 말하는 구간만 잘라내기
- [ ] 잘라낸 구간을 faster-whisper 에 넘기기
- [ ] **모델은 한 번만 불러오고 재사용** (매번 부르면 5초씩 낭비)
- [ ] `config.yaml` 의 `initial_prompt` 를 실제로 적용
- [ ] **지연 측정 코드를 지금 넣기** — 말이 끝난 시각 → 자막이 뜬 시각
  ```python
  latency = time.time() - speech_end_time
  ```
- [ ] **오늘의 성공:** 말하면 몇 초 뒤 터미널에 계속 자막이 뜬다

## 함도유 — 화면

- [ ] PySide6 로 창 하나 띄우기
- [ ] 자막이 들어갈 큰 글씨 영역 만들기 (`config.yaml` 의 `ui.font_size`)
- [ ] 방향 화살표 그리기
- [ ] `tuning.py` 에서 각도 읽어 화살표 돌리기
  ```python
  screen_angle = (90 - mic_angle) % 360
  ```
- [ ] **`is_voice()` 가 참일 때만 갱신** — 안 그러면 화살표가 제멋대로 돈다
- [ ] **오늘의 성공:** 말하는 방향으로 화살표가 돈다 (자막은 가짜 글씨여도 됨)

## 저녁에 다 같이 (1시간)

- [ ] 각자 본인 브랜치에 커밋·푸시
- [ ] **작성자 확인** — `git log -1 --format="%an <%ae>"`
- [ ] 📸 **참가자 사진 촬영** (전원) — 요구 규격 확인하고 찍기
- [ ] 시연 영상 구성 확정 (아래 7/24 참고)
- [ ] 작품요약서 초안 읽고 고칠 곳 표시

---

# 7/21 (화) — 합치기 · 가장 중요한 날

> 세 조각이 한 화면에서 같이 돌아야 합니다.
> **합치는 건 항상 예상보다 오래 걸립니다.** 각자 만든 게 서로 충돌합니다.

## 다 같이 (오전)

- [ ] 세 브랜치를 하나로 합칠 방법 정하기 (`main` 으로 병합)
- [ ] 충돌 나는 부분 같이 보기
- [ ] `src/main.py` 에서 세 기능을 동시에 돌리기

## 통합 순서 (권장)

- [ ] 1단계: 자막만 화면에 띄우기 (터미널 → UI)
- [ ] 2단계: 방향 화살표 붙이기
- [ ] 3단계: 입력창 붙이기 (타이핑 → TTS)
- [ ] 각 단계마다 **동작 확인하고 커밋**

## 강주언 — I2S 스피커 배선

- [ ] MAX98357A 배선 (VIN→5V, GND, BCLK, LRC, DIN)
- [ ] `sudo /opt/nvidia/jetson-io/jetson-io.py` 로 I2S 활성화 → **재부팅**
- [ ] `aplay -l` 로 카드 번호 확인
- [ ] `config.yaml` 의 `tts.output_device` 채우기 (**card 2 아님** — 그건 마이크)
- [ ] 실제로 스피커에서 소리 나는지 확인

## 저녁

- [ ] 통합본 커밋·푸시
- [ ] 🎬 **영상 촬영 장소·시간 확정** (교실 사용 가능한지 확인)

> ⚠️ 화요일에 통합이 안 되면 **수요일 오전까지** 씁니다. 그래서 여유를 뒀습니다.
> 다만 수요일 오후에는 반드시 측정으로 넘어가야 합니다.

---

# 7/22 (수) — 숫자 만들기

> "동작한다"로는 부족합니다. **숫자로 증명**해야 합니다. (CLAUDE.md 8장)
> 이날 나온 표가 발표의 핵심입니다.

## 준비 (오전, 다 같이)

- [ ] `docs/04-experiment.md` 를 먼저 확정 — 무엇을, 몇 번, 어떤 기준으로 잴지
- [ ] 읽을 문장 20개 정하기 (교과서 문장, 길이 비슷하게)
- [ ] 조용한 조건 / 소음 조건 정의하기 (소음은 어떻게 만들 것인가)
- [ ] 측정 결과를 적을 표 미리 만들기

## 측정 ① WER (자막 정확도) — 강구현

- [ ] 조용한 조건에서 문장 20개 읽고 인식 결과 저장
- [ ] 소음 조건에서 같은 문장 20개
- [ ] `initial_prompt` **있을 때 / 없을 때** 각각 (이게 우리의 발견)
- [ ] 틀린 단어 수 세어 WER 계산
- [ ] 표로 정리

## 측정 ② 자막 지연 — 강구현

- [ ] 1패스 지연 20회 측정 → 평균·중앙값
- [ ] 2패스 지연 20회 측정 → 평균·중앙값

## 측정 ③ DOA 오차 — 함도유

- [ ] 바닥에 **각도 표시** (0·45·90·135·180·225·270·315)
- [ ] 각 방향에서 5초씩 발성, 목소리 감지된 값만 수집
- [ ] 위치마다 **3회 이상** 반복
- [ ] 실제 각도와 측정 각도의 차이 계산 → 평균 오차
- [ ] `docs/13-doa-calibration.md` 갱신

## 측정 ④ TTS 지연 — 강주언

- [ ] 엔터 → 소리 시작까지 20회 측정
- [ ] 문장 길이별로 나눠서도 측정

## 저녁

- [ ] 원자료를 `experiments/data/` 에 저장 — **⚠️ 커밋 금지** (`.gitignore` 확인)
- [ ] 표만 문서에 옮기기
- [ ] 작품요약서의 `【   】` 빈칸 채우기

---

# 7/23 (목) — 문서 쓰는 날

> **코드는 손대지 않습니다.** 오늘 하루는 글만 씁니다.

## 작품요약서 (마무리) — 다 같이

- [ ] 수요일 측정값 반영
- [ ] **A4 1장 넘지 않는지** 확인
- [ ] 팀명·소속·지도교사 정보 맞는지 확인
- [ ] 오탈자 검토

## 작품설명서 + 소스코드 — 강구현 주도

- [ ] 공식 서식의 항목 구성 확인
- [ ] 문제 정의 (`docs/01-problem.md` 활용)
- [ ] 시스템 설계 (`docs/02-system-design.md` + 도식 3장)
- [ ] 하드웨어 구성·배선 (`docs/03-hardware.md`)
- [ ] 구현 설명 — **2-패스가 왜 필요한가**를 반드시 설명
- [ ] 측정 결과
- [ ] **소스코드 첨부**
- [ ] 🔴 **출처 표기 전수 점검** — 미표기는 규정 위반
  - `usb_4_mic_array` (Apache-2.0) + `tostring→tobytes` 수정 사실
  - faster-whisper, Silero VAD, MeloTTS 각각의 라이선스
  - AI 도구를 썼다면 그 사실도 (규정 확인 후)

## 발표자료 — 함도유 주도

- [ ] **4분 분량**으로 구성

  | 시간 | 내용 |
  |---|---|
  | 0:00~0:40 | 문제 — 당사자 자문 "자막보다 누가 말하는지가 급하다" |
  | 0:40~1:10 | 기존 제품과의 차이 — 완전 오프라인 |
  | 1:10~2:30 | 어떻게 만들었나 — 2-패스, DOA, 단말 내장 스피커 |
  | 2:30~3:20 | **측정 결과** (수요일의 숫자) |
  | 3:20~4:00 | 한계와 다음 단계 |

- [ ] 글씨 크게, 한 장에 한 메시지
- [ ] `initial_prompt` 발견을 **한 장으로** 만들기 (우리만의 결과)

## 저녁 — 반드시 할 것

- [ ] 🔴 **PDF 변환 시험** — 한글 폰트 깨짐·표 밀림 확인
  > 토요일 밤에 처음 변환하다 문제가 생기면 손쓸 시간이 없습니다.
- [ ] 발표 1회 연습 (타이머 켜고)

---

# 7/24 (금) — 🔨 개발 마감 · 촬영

## 오후까지 — 마지막 코드 작업

- [ ] 시연에서 보여줄 부분만 집중 점검
- [ ] 눈에 띄는 버그 중 **10분 안에 고칠 수 있는 것만** 고치기
- [ ] 그 외는 손대지 않기 — 고치다 더 망가지는 게 가장 위험
- [ ] 🔴 **저녁 이후 코드 동결**
- [ ] 최종 커밋·푸시, 세 브랜치를 `main` 으로 병합

## 저녁 — 🎬 시연 영상 촬영 (최장 2분)

- [ ] **와이파이를 끄고** 촬영 시작 — 그 화면 자체가 증거
- [ ] 촬영 구성

  | 시간 | 장면 |
  |---|---|
  | 0:00~0:20 | 문제 상황 |
  | 0:20~1:00 | 자막이 뜨는 장면 (1패스 → 2패스 교정이 보이게) |
  | 1:00~1:30 | 방향 화살표가 도는 장면 |
  | 1:30~1:50 | 타이핑 → 기기가 말하는 장면 |
  | 1:50~2:00 | 오프라인 강조 |

- [ ] **여러 번 찍기** — 편집은 토요일
- [ ] 소리가 제대로 녹음됐는지 그 자리에서 확인
- [ ] 화면이 잘 보이는지 확인 (자막 글씨가 영상에서 읽히는가)

> 2-패스 교정 장면이 가장 중요합니다. **틀린 자막이 맞게 바뀌는 순간**이 보여야 합니다.

---

# 7/25 (토) — 마무리

## 영상 — 함도유

- [ ] 편집
- [ ] 🔴 **2분 이하 확인** (초과하면 잘립니다)
- [ ] 자막·설명 넣기 (소리 없이 봐도 이해되게)
- [ ] 다른 컴퓨터에서 재생 테스트

## 문서 — 강구현

- [ ] 작품설명서 완성
- [ ] 소스코드 최종본 첨부
- [ ] **PDF 변환** 후 눈으로 전체 확인
- [ ] 출처 표기 재확인

## 발표 — 강주언

- [ ] 발표자료 완성 → PDF
- [ ] 🔴 **발표 연습 3회 이상, 타이머 켜고**
- [ ] **4분 넘으면 내용을 줄이기** (빨리 말하지 말 것)
- [ ] 예상 질문 10개 뽑고 답 준비

## 예상 질문 (미리 답을 만들어두세요)

- [ ] "왜 클라우드를 안 쓰나요?"
- [ ] "2-패스가 정확히 어떻게 동작하나요?"
- [ ] "`initial_prompt` 는 뭔가요? 왜 효과가 있나요?"
- [ ] "방향은 어떻게 계산하나요?" (→ 칩이 한다. 우리는 읽는다)
- [ ] "스피커를 왜 단말에 넣었나요?"
- [ ] "정확도가 낮은 조건은 언제인가요?"
- [ ] "실제 청각장애 학생에게 써봤나요?"
- [ ] "만들지 못한 기능은 무엇인가요?"

> **모르면 "모른다"고 하세요.** 지어내면 그때 무너집니다.

---

# 7/26 (일) — 제출

> **낮에 제출하세요.** 23:59는 마지노선이지 목표가 아닙니다.

## 오전 — 최종 점검

- [ ] 발표자료 (PDF)
- [ ] 작품요약서 (PDF) — A4 1장
- [ ] 작품설명서 + 소스코드 (PDF)
- [ ] 시연 동영상 — **2분 이하**
- [ ] 참가자 사진 — **전원**
- [ ] 파일명이 요구 형식에 맞는지

## 🔴 실격 방지 점검 — 반드시 눈으로

- [ ] 저장소에 **개인정보가 담긴 파일이 없는지** (참가신청서·동의서·연락처)
- [ ] `CONTEXT.local.md` 가 `.gitignore` 되어 있는지
- [ ] `experiments/data/` 가 커밋되지 않았는지
- [ ] **모든 커밋 작성자가 팀원 계정인지**
  ```bash
  git log --format="%an <%ae>" | sort -u
  ```
  팀원 셋 외의 이름이 나오면 **즉시 알리세요**
- [ ] 출처 표기 누락이 없는지

## 오후 — 제출

- [ ] 제출
- [ ] **제출 완료 화면 캡처해서 보관**
- [ ] 팀 대화방에 완료 공유

---

# 이후 — 8/1~8/2 ZOOM 심사

- [ ] 전원 참석 가능한지 재확인 (**1명이라도 빠지면 전원 탈락**)
- [ ] 각자 인터넷·마이크·카메라·화면공유 테스트
- [ ] 발표 순서와 역할 분담 확정
- [ ] 리허설 (발표 4분 + 질의응답 4분)

---

# 매일 지킬 것

- [ ] **30분 넘게 막히면 팀에 말하기** — 혼자 두 시간 헤매지 말 것
- [ ] 그날 한 것 **반드시 푸시** — Jetson만 믿지 말 것
- [ ] 새로 알게 된 것은 **그날 문서에 적기** — 며칠 지나면 잊습니다
- [ ] 설명하지 못하는 코드는 커밋하지 않기

---

## 시간이 부족하면 버릴 순서

만약 일정이 밀리면, 아래 순서로 포기하세요.

1. TTS 음질 개선 (`espeak-ng` 로 충분)
2. UI 꾸미기
3. 소음 조건 WER 측정
4. ~~자막~~ ~~방향~~ ~~시연 영상~~ ~~문서~~ ← **이 넷은 절대 버리지 않는다**

**화려한 코드가 아니라 카메라 앞에서 확실히 동작하는 것이 목표입니다.**
